# Database Backup Configuration
# Copy this file to .backup.env and fill in your actual values
# DO NOT commit .backup.env to version control!

# Database connection URL (from your .env.local)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://username:password@localhost:5432/your_database

# AWS S3 Configuration
S3_BUCKET_NAME=your-backup-bucket-name
AWS_REGION=us-east-1

# AWS Credentials (if not using IAM role)
# If your EC2 instance has an IAM role with S3 access, you can leave these empty
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key

# Backup Configuration
BACKUP_DIR=/tmp/db-backups
RETENTION_DAYS=7  # Keep backups for 7 days
LOG_FILE=/var/log/db-backup.log

# Optional: SNS Topic for notifications
# SNS_TOPIC_ARN=arn:aws:sns:us-east-1:123456789:db-backup-alerts
